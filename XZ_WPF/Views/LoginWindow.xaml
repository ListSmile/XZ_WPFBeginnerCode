<Window
    x:Class="XZ_WPF.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:XZ_WPF.Views"
    xmlns:local_pwd="clr-namespace:XZ_WPF.Common"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xzbtn="clr-namespace:XZ_WPF"
    Name="LoginView"
    Title="LoginWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            Margin="5"
            HorizontalAlignment="Center"
            FontFamily="Arial"
            FontSize="26"
            Foreground="Blue"
            Text="XZ-数据资源管理系统" />
        <StackPanel Grid.Row="1" Background="#0078d4">
            <TextBlock
                Margin="5"
                HorizontalAlignment="Center"
                FontSize="22"
                Foreground="White"
                Text="登录" />
        </StackPanel>
        <Grid
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                VerticalAlignment="Center"
                FontSize="16"
                Text="用户名：" />
            <xzbtn:XZTextBox
                Grid.Row="0"
                Grid.Column="1"
                Margin="5"
                Text="{Binding UserName}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="5"
                VerticalAlignment="Center"
                FontSize="16"
                Text="密码：" />
            <PasswordBox
                Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                local_pwd:PassWordHelper.Attach="True"
                local_pwd:PassWordHelper.XZPassWord="{Binding PassWord, Mode=TwoWay}" />
            <CheckBox
                Grid.Row="2"
                Grid.Column="1"
                Grid.ColumnSpan="2"
                Margin="5"
                Content="记住密码" />
            <xzbtn:XZButton
                Grid.Row="3"
                Grid.Column="1"
                Width="190"
                Margin="5"
                HorizontalAlignment="Center"
                BackGroundHover="Red"
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=LoginView}"
                Content="登录"
                XZCornerRadius="10" />
        </Grid>
    </Grid>
</Window>
